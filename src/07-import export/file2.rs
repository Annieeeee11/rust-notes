/* The module’s items (functions, structs, etc.) are private by default — you need to mark them with pub if you want to access them from outside. */ 
pub fn testfunction1(a: u32, b: u32) -> u32 {
    a + b
}