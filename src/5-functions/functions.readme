Talking about function 
- the "main" function, which is the entry point of many programs
- "fn" keyword -- allows you to declare new functions.

- uses snake case as the conventional style for function and variable names ( lowercase, underscore -- user_example)